import React from 'react';
// import { useParams } from 'react-router-dom';

const deleteTable = () => {
    // const params = useParams();

    return(
        <div>
            <h1>Table Deleted!</h1>
        </div>
        // <React.Fragment>
        //     <h1>{params.id} was successfully deleted.</h1>
        // </React.Fragment>
    );
};

export default deleteTable;